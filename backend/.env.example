# ==========================================
# BIBLIOTECA DIGITAL - CONFIGURAÇÃO
# ==========================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo: cp .env.example .env
# 2. Preencha os valores marcados com <PREENCHER>
# 3. Nunca commite o arquivo .env no Git
#
# ==========================================

# ------------------------------------------
# APLICAÇÃO
# ------------------------------------------
# Ambiente de execução: development, production, test
NODE_ENV=development

# Porta onde o servidor irá rodar
PORT=3000

# ------------------------------------------
# BANCO DE DADOS
# ------------------------------------------
# String de conexão PostgreSQL
# Substitua: username, password, database_name
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# ------------------------------------------
# SEGURANÇA - JWT
# ------------------------------------------
# Chave secreta para assinar tokens JWT
# ⚠️ IMPORTANTE: Gere uma chave forte com o comando abaixo:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<PREENCHER_COM_HASH_GERADO>

# Tempo de expiração do token (exemplos: 1h, 7d, 30d)
JWT_EXPIRES_IN=7d

# ------------------------------------------
# UPLOAD DE ARQUIVOS
# ------------------------------------------
# Tamanho máximo de arquivo em bytes
# 5MB = 5242880 bytes
# 10MB = 10485760 bytes
MAX_FILE_SIZE=5242880

# Diretório para salvar uploads
UPLOAD_PATH=./uploads

# ------------------------------------------
# REGRAS DE NEGÓCIO
# ------------------------------------------
# Prazo padrão de empréstimo (em dias)
DEFAULT_LOAN_DAYS=14

# Número máximo de renovações permitidas
MAX_RENEWALS=2

# Valor da multa por dia de atraso (em reais)
FINE_PER_DAY=2.50

# ------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# ------------------------------------------
# URLs permitidas para fazer requisições (separadas por vírgula)
# Exemplo: http://localhost:3000,https://meusite.com
CORS_ORIGIN=http://localhost:3000

# ------------------------------------------
# OPCIONAL: EMAIL
# ------------------------------------------
# Descomente e configure para habilitar notificações por email

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=<SEU_EMAIL>
# SMTP_PASS=<SENHA_DO_APP>
# EMAIL_FROM=noreply@biblioteca.com

# ------------------------------------------
# OPCIONAL: LOGS
# ------------------------------------------
# Nível de detalhamento: error, warn, info, debug
LOG_LEVEL=info